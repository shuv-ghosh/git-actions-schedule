name: Manual Workflow

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *'
    
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings
        run: echo "Run on Schedule"
